services:
  database:
    container_name: apidb
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: apidb
    volumes:
      - apidb:/var/lib/postgresql/data

volumes:
  apidb:
