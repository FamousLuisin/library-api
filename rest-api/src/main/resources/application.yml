spring:
  application:
    name: rest-api
  datasource:
    url: jdbc:postgresql://localhost:5432/apidb
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
logging:
  level:
    root: warn