server:
  port: 8888
spring:
  application:
    name: rest-api
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
logging:
  level:
    root: info
springdoc:
  paths-to-match:
  - /api/**
  swagger-ui:
    use-root-path: true